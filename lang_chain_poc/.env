env:
# For Databricks, consider storing secrets in Databricks Secrets and reading them at runtime.
# If running locally, copy to .env (gitignore it) and fill values.
OPENAI_API_KEY=""
OPENAI_MODEL=gpt-4o-mini

PINECONE_API_KEY=""
PINECONE_ENVIRONMENT=us-east-1
PINECONE_INDEX_NAME=langchain-poc-index

TAVILY_API_KEY=""

DEFAULT_K=6
CHUNK_SIZE=1200
CHUNK_OVERLAP=200

HUGGINGFACEHUB_API_TOKEN=""
EMBEDDING_MODEL="hf:bge-base-en-v1.5"
OPENAI_EMBEDDING_MODEL=hf:bge-base-en-v1.5   # (we just reuse this var name)
ALPHAVANTAGE_API_KEY="" # not needed
FINNHUB_API_KEY="" # not needed
